# Composition Schemas

## DocumentComposition
Contains physical `PageComposition`s and mode settings.

## PageComposition
Contains `PageRegion`s, which map to abstract sections like `HEADER`, `PRIMARY`, etc.

## RegionRole
Enumeration of semantic regions.
