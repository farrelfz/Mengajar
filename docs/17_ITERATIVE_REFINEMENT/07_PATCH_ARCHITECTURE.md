# PATCH ARCHITECTURE & IMMUTABLE DERIVATION
## BATCH 17 — ITERATIVE REFINEMENT & CLOSED-LOOP MATERIAL IMPROVEMENT

### 1. The Patch Abstraction (`app/refinement/patches.py`)
All refinement actions produce immutable derivative candidates:
$$\text{Candidate} = \text{apply}(\text{Baseline}, \text{Patch})$$

### 2. Concrete Patch Operations
- `PedagogicalSequencePatch`: Reorders journey stages.
- `DensitySplitPatch`: Splits overloaded blocks across pages.
- `CapabilityReplacementPatch`: Upgrades visual grammar topologies.
- `RedundancyDeduplicationPatch`: Condenses repeated spans into summary callouts.
